# PerspectivesOA

SimplePractice clinical extraction + ASAM 4e / TJC CTS audit service.

Project scaffold — implementation in flight. See `pyproject.toml` for the
toolchain (`uv`, Python 3.12, FastAPI, SQLAlchemy, Pydantic v2) and the
`Makefile` for the dev / test / migrate targets.

Documentation will be filled in as the components land.
